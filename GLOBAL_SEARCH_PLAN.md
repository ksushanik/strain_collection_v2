# План реализации глобального поиска "как Google" для коллекции штаммов

## Обзор задачи
Реализовать глобальный поиск на главной странице с гибридным UX:
- Ввод → мгновенные подсказки в выпадающем списке под полем (preview)
- Enter → раскрывает полноценную панель с результатами (full)
- Группировка по разделам: штаммы, образцы, хранилища, среды, методы, легенда, wiki
- Клик по результату → переход в соответствующий раздел

## Статус реализации

### ✅ Готово (Backend)
1. **Модуль поиска**: `backend/src/search/`
   - `search.module.ts` - модуль с зависимостями (Prisma, Casl, Settings)
   - `search.controller.ts` - контроллер `/api/v1/search` с проверкой прав
   - `search.service.ts` - сервис с логикой агрегации по секциям
   - `search.types.ts` - типы для ответа API
   - `dto/search-query.dto.ts` - DTO с валидацией параметров

2. **Поддерживаемые секции поиска**:
   - **strains**: identifier, ncbiScientificName, taxonomy16s, otherTaxonomy, features, comments, sample.code
   - **samples**: code, siteName, subject, description
   - **storage**: displayName, description (боксы) + cellCode, strain.identifier (ячейки)
   - **media**: name, composition
   - **methods**: name, description
   - **legend**: content (Markdown)
   - **wiki**: файлы из `docs/wiki/` по списку `WIKI_DOCS`

3. **Безопасность**: CASL-фильтрация по правам пользователя
4. **API**: GET `/api/v1/search?query=...&mode=preview|full&perSection=3|10`

### ✅ Готово (Frontend API)
1. **Сервис**: `frontend/src/services/search.ts`
   - `searchGlobal(params)` - вызов бэкенда
   - Интеграция в `frontend/src/services/api.ts`
2. **Типы**: `GlobalSearchResponse` в типах API

### 🔄 Осталось сделать

#### 1. Frontend: UI компоненты GlobalSearch
**Задача**: Создать `frontend/src/components/domain/global-search/`

**Компоненты**:
- `GlobalSearch.tsx` - основной компонент
- `SearchCommand.tsx` - выпадающий список с Command (shadcn)
- `SearchResultsPanel.tsx` - полноэкранная панель результатов
- `SearchSection.tsx` - компонент секции результатов
- `SearchHitItem.tsx` - отдельный элемент результата

**Технические детали**:
- Использовать shadcn `Command`, `Dialog`, `Badge`
- Состояния: `query`, `debouncedQuery` (300мс), `mode`, `results`, `isPanelOpen`
- Хоткей `Cmd+K` для открытия
- Группировка результатов с иконками по секциям
- AbortController для отмены запросов
- Адаптивность (Drawer на мобиле)

#### 2. Frontend: Интеграция на главную
**Задача**: Встроить GlobalSearch в `frontend/src/app/[locale]/page.tsx`

**Требования**:
- Разместить поиск над карточками статистики
- Сохранить существующую верстку
- Обеспечить работу на всех устройствах
- Использовать текущие стили и темы

#### 3. Frontend: Типы для поиска
**Задача**: Добавить типы в `frontend/src/types/api/index.ts`

**Типы**:
```typescript
export type SearchSection = 
  | 'strains' | 'samples' | 'storage' | 'media' 
  | 'methods' | 'legend' | 'wiki';

export type SearchHit = {
  section: SearchSection;
  id: string;
  title: string;
  snippet?: string | null;
  href: string;
  metadata?: Record<string, unknown>;
};

export type GlobalSearchResponse = {
  query: string;
  mode: 'preview' | 'full';
  perSection: number;
  sections: Record<SearchSection, SearchHit[]>;
};
```

## Архитектура решения

### Backend (NestJS + Prisma)
- **Агрегация**: `SearchService.search()` выполняет параллельные запросы к доменным сервисам
- **Фильтрация**: Проверка прав через CASL перед выполнением запросов
- **Лимиты**: `perSection` (preview=3, full=10) для ограничения результатов
- **Обработка ошибок**: `Promise.allSettled` для изоляции сбоев секций
- **Wiki**: Чтение файлов из `docs/wiki/` через `fs/promises`
- **Сниппеты**: Функция `makeSnippet()` для выделения контекста

### Frontend (Next.js + shadcn/ui)
- **Command Pattern**: Использование готового `Command` компонента для autocomplete
- **Dialog/Sheet**: Полноэкранная панель для детальных результатов
- **Debounce**: Задержка 300мс для оптимизации запросов
- **Состояния**: React hooks для управления UI
- **Маршрутизация**: `router.push()` для навигации по результатам

## Ссылки на ключевые файлы

### Backend
- [`backend/src/search/search.service.ts`](backend/src/search/search.service.ts) - основная логика
- [`backend/src/search/search.controller.ts`](backend/src/search/search.controller.ts) - API endpoint
- [`backend/src/app.module.ts`](backend/src/app.module.ts) - регистрация модуля

### Frontend
- [`frontend/src/services/search.ts`](frontend/src/services/search.ts) - API клиент
- [`frontend/src/services/api.ts`](frontend/src/services/api.ts) - интеграция в ApiService

## Примеры API

### Preview (выпадающий список)
```
GET /api/v1/search?query=escherichia&mode=preview
```
```json
{
  "query": "escherichia",
  "mode": "preview", 
  "perSection": 3,
  "sections": {
    "strains": [
      {
        "section": "strains",
        "id": "123",
        "title": "E.coli K12",
        "snippet": "Sample: 24-PL-Rose",
        "href": "/strains/123"
      }
    ],
    "samples": [...],
    "storage": [...],
    "media": [...],
    "methods": [...],
    "legend": [...],
    "wiki": [...]
  }
}
```

### Full (полная панель)
```
GET /api/v1/search?query=escherichia&mode=full&perSection=10
```

## UX Сценарии

### Сценарий 1: Быстрый поиск
1. Пользователь начинает вводить текст
2. Через 300мс появляется выпадающий список с подсказками
3. Подсказки группированы по секциям (до 3 на секцию)
4. Клик по результату → закрытие поиска и переход

### Сценарий 2: Детальный поиск
1. Пользователь вводит текст и нажимает Enter
2. Открывается полноэкранная панель результатов
3. Показываются расширенные списки (до 10 на секцию)
4. Каждый результат содержит сниппет с контекстом
5. Возможность перехода в любой раздел

### Сценарий 3: Навигация
- Клавиши ↑↓ для навигации по результатам
- Enter для выбора
- Esc для закрытия
- Cmd+K для быстрого открытия

## Техническая реализация

### Backend: Поисковые стратегии

#### Strains
```typescript
where: {
  OR: [
    { identifier: { contains: q, mode: 'insensitive' } },
    { ncbiScientificName: { contains: q, mode: 'insensitive' } },
    { taxonomy16s: { contains: q, mode: 'insensitive' } },
    { otherTaxonomy: { contains: q, mode: 'insensitive' } },
    { features: { contains: q, mode: 'insensitive' } },
    { comments: { contains: q, mode: 'insensitive' } },
    { sample: { is: { code: { contains: q, mode: 'insensitive' } } } },
  ],
}
```

#### Storage (двойная логика)
- **Боксы**: поиск по `displayName`, `description`
- **Ячейки**: поиск по `cellCode` + связанный `strain.identifier`

#### Wiki
- Чтение файлов из `docs/wiki/`
- Индексация заголовков и содержимого
- Поддержка `WIKI_DOCS` списка

### Frontend: Состояния компонента
```typescript
interface SearchState {
  query: string;
  debouncedQuery: string;
  mode: 'preview' | 'full';
  results: GlobalSearchResponse | null;
  isPanelOpen: boolean;
  isLoading: boolean;
  error: string | null;
}
```

## Безопасность и производительность

### Backend
- **CASL**: Фильтрация секций по правам пользователя
- **Throttling**: Ограничение частоты запросов (можно добавить)
- **Минимальная длина**: query >= 2 символа
- **Лимиты**: `perSection` для контроля нагрузки
- **Обработка ошибок**: Изоляция сбоев секций

### Frontend
- **Debounce**: 300мс задержка для снижения нагрузки
- **AbortController**: Отмена предыдущих запросов
- **Ленивая загрузка**: Результаты загружаются по требованию
- **Кэширование**: Опционально для повторных запросов

## Следующие шаги для разработчика

1. **Создать UI компоненты**:
   - `GlobalSearch.tsx` - основной компонент
   - `SearchCommand.tsx` - выпадающий список
   - `SearchResultsPanel.tsx` - полноэкранная панель

2. **Интегрировать в главную страницу**:
   - Модифицировать `frontend/src/app/[locale]/page.tsx`
   - Добавить компонент над статистикой

3. **Добавить типы**:
   - Обновить `frontend/src/types/api/index.ts`

4. **Протестировать**:
   - Unit тесты для сервиса
   - E2E тесты для UI сценариев
   - Проверить производительность

## Готовность
- **Backend**: 100% готов
- **Frontend API**: 100% готов  
- **Frontend UI**: 0% готов
- **Интеграция**: 0% готова
- **Общая готовность**: ~60%